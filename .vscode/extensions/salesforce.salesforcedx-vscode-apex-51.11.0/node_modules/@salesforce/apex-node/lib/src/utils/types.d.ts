declare enum logLevel {
    trace = "trace",
    debug = "debug",
    info = "info",
    warn = "warn",
    error = "error",
    fatal = "fatal"
}
export declare type CommonOptions = {
    json?: boolean;
    loglevel?: logLevel;
};
export declare type ApexDiagnostic = {
    lineNumber?: number;
    columnNumber?: number;
    exceptionMessage: string;
    compileProblem: string;
    exceptionStackTrace: string;
    className?: string;
};
export {};
