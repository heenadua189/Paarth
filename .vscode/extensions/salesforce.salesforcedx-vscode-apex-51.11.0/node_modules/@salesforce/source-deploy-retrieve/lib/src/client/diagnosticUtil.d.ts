import { ComponentDeployment, DeployMessage, ComponentRetrieval } from './types';
export declare class DiagnosticUtil {
    private api;
    constructor(api: 'metadata' | 'tooling');
    setRetrieveDiagnostic(message: string, componentRetrieval: ComponentRetrieval): ComponentRetrieval;
    setDeployDiagnostic(componentDeployment: ComponentDeployment, message: string | DeployMessage): ComponentDeployment;
    private parseLwc;
    private parseAura;
    private parseDefault;
}
