import { SourcePath } from '../common';
import { SourceComponent } from '../metadata-registry';
import { BaseApi, RetrieveOptions, RetrievePathOptions, ToolingDeployOptions, SourceDeployResult, SourceRetrieveResult } from './types';
export declare const deployTypes: Map<string, string>;
export declare class ToolingApi extends BaseApi {
    retrieveWithPaths(options: RetrievePathOptions): Promise<SourceRetrieveResult>;
    retrieve(options: RetrieveOptions): Promise<SourceRetrieveResult>;
    deploy(components: SourceComponent | SourceComponent[], options?: ToolingDeployOptions): Promise<SourceDeployResult>;
    deployWithPaths(path: SourcePath, options?: ToolingDeployOptions): Promise<SourceDeployResult>;
}
