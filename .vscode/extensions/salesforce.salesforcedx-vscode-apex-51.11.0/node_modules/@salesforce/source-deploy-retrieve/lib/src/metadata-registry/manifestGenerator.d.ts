import { MetadataComponent } from '../common';
import { MetadataResolver } from './metadataResolver';
import { RegistryAccess } from './registryAccess';
export declare class ManifestGenerator {
    private packageModuleStart;
    private packageModuleEnd;
    private resolver;
    private registry;
    constructor(resolver?: MetadataResolver, registry?: RegistryAccess);
    createManifestFromPath(sourcePath: string, outputPath: string): void;
    createManifest(components: MetadataComponent[], apiVersion?: string, indent?: string): string;
    private createMetadataMap;
}
